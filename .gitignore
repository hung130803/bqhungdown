node_modules/
dist/
dist-web/
src-tauri/target/

# Updater signing keys — NEVER commit these
src-tauri/.tauri-updater-key
src-tauri/.tauri-updater-key.pub

# Tauri build artifacts
src-tauri/gen/
src-tauri/Cargo.lock.bak

# Editor
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# File SINH RA khi build — không commit.
# CẢNH BÁO đã gặp: Vite ưu tiên `vite.config.js` TRƯỚC `vite.config.ts`, nên
# nếu bản .js commit sẵn bị cũ thì build âm thầm dùng cấu hình cũ, sửa .ts
# không có tác dụng. Để tsc tự sinh mỗi lần build là chắc chắn không lệch.
vite.config.js
vite.config.d.ts
*.tsbuildinfo
